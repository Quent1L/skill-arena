{
  "name": "skill-arena-monorepo",
  "version": "1.0.0",
  "description": "Skill Arena - Tournament Management Application",
  "private": true,
  "scripts": {
    "dev": "bun run dev:setup && bun run dev:all",
    "dev:setup": "echo '🔧 Initial build of shared package...' && cd shared && bun run build",
    "dev:all": "concurrently \"bun run dev:shared\" \"bun run dev:backend\" \"bun run dev:frontend\" --names \"shared,backend,frontend\" --prefix-colors \"blue,green,yellow\"",
    "dev:shared": "cd shared && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "dev:frontend": "cd frontend && bun run dev",
    "restart": "./restart-dev.sh",
    "start": "bun run dev",
    "build": "bun run build:shared && bun run build:frontend",
    "build:shared": "cd shared && bun run build",
    "build:backend": "cd backend && echo 'Backend does not need build step'",
    "build:frontend": "cd frontend && bun run build",
    "install:all": "bun install && cd shared && bun install && cd ../backend && bun install && cd ../frontend && bun install",
    "clean": "bun run clean:shared && bun run clean:frontend",
    "clean:shared": "cd shared && bun run clean",
    "clean:frontend": "cd frontend && rm -rf dist",
    "type-check": "bun run type-check:shared && bun run type-check:backend && bun run type-check:frontend",
    "type-check:shared": "cd shared && tsc --noEmit",
    "type-check:backend": "cd backend && tsc --noEmit",
    "type-check:frontend": "cd frontend && bun run type-check",
    "lint": "cd frontend && bun run lint",
    "format": "cd frontend && bun run format"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ]
}
