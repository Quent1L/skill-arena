{
  "name": "backend",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "type-check": "tsc --noEmit",
    "build": "tsc --build",
    "db:generate": "bun run drizzle-kit generate",
    "db:push": "bun run drizzle-kit push",
    "job:auto-finalize": "bun run src/jobs/auto-finalize-matches.job.ts",
    "test": "bun run test:unit && bun run test:integration",
    "test:unit": "bun test src/services/__tests__/*.test.ts src/utils/__tests__/*.test.ts --isolate",
    "test:cov": "bun test --isolate --coverage --coverage-reporter=text --coverage-reporter=lcov src/services/__tests__/*.test.ts src/utils/__tests__/*.test.ts src/services/__tests__/integration/*.test.ts",
    "test:integration": "bun test src/services/__tests__/integration/*.test.ts --isolate",
    "lint": "eslint . --fix",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "@skol-arena/shared": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "graphile-worker": "^0.17.3",
    "hono": "^4.12.31",
    "i18next": "^26.3.6",
    "i18next-http-middleware": "^3.9.7",
    "json-rules-engine": "^7.3.1",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0",
    "pino": "^10.3.1",
    "web-push": "^3.6.7",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@types/bun": "^1.3.14",
    "@types/nodemailer": "^8.0.1",
    "@types/pg": "^8.20.0",
    "@types/web-push": "^3.6.4",
    "drizzle-kit": "^0.31.10",
    "eslint": "^10.7.0",
    "prettier": "^3.9.5",
    "tsx": "^4.23.1",
    "typescript": "catalog:",
    "typescript-eslint": "^8.64.0"
  }
}
